---
title: 情绪陪伴多智能体系统
summary: 家育宝「智心羽 AI」的算法侧。BERT 意图三分类路由 + 基于 LangGraph 的三阶段情绪路由 + 14 个「物件人格」角色化 Agent，配合双层长期记忆与两级危机兜底，做成一个已上线的 K12 心理健康陪伴产品。
tags: ['多智能体', 'LLM 工程']
role: 算法实习生
period: '2026.01 – 2026.04'
order: 30
featured: false
metrics:
  - { label: '角色化 Agent', value: '14' }
  - { label: '意图路由', value: '三分类' }
  - { label: '情绪路由阶段', value: '摘要 / 信号 / 推荐' }
  - { label: '长期记忆', value: '双层' }
  - { label: '危机检测', value: '两级' }
  - { label: '状态', value: '已上线' }
stack: ['LangGraph', 'BERT', 'Mem0', 'SQLite', 'Elasticsearch', 'vLLM', 'FastAPI']
links:
  - { label: 'GitHub', href: 'https://github.com/JACKEYED' }
---

## 背景

「智心羽 AI」是家育宝面向 K12 学生与家长的对话式心理健康产品，已在各大应用商店上线。我在实习期间负责多智能体对话系统的算法侧——从意图路由、情绪路由、角色 Agent，到长期记忆和安全兜底。

这个场景有几个特殊约束，决定了后面的所有设计取舍：

- **用户是未成年人**，误路由和有害输出的代价很高；
- **陪伴 ≠ 问答**，用户要的往往不是"正确答案"，而是"被接住"；
- **危机内容必须零幻觉**——援助热线号码绝不能让 LLM 生成。

## 系统总览

```text
用户消息 (user_id, session_id, message)
      │
      ▼
┌──────────────────────┐   业务 / 心理健康 / 通用
│  BERT 意图三分类路由   │───────────────┐
└──────────┬───────────┘               │
           ▼ (心理健康)                  ▼
┌─────────────────────────────┐   业务 Agent / 通用 Agent
│  情绪路由 Agent              │
│  ① 会话摘要 ② 情绪信号 ③ 推荐 │
└───────┬──────────────┬──────┘
        │ handoff      │ 情绪回流
        ▼              │
┌───────────────────┐  │      ┌────────────────────┐
│  物件人格 Agent    │──┘      │ 双层长期记忆        │
│  14 个独立子图     │─────────│ Mem0 + SQLite      │
│  纸巾盒/梳子/沙袋… │         │ 异步事实抽取更新    │
└─────────┬─────────┘         └────────────────────┘
          ▼
     HTML 技能小游戏（部分角色）

贯穿全程：两级危机检测（关键词 + LLM 复核）
```

算法端在重构后**完全无状态**：每次请求靠入参驱动，历史 / 状态 / 长期记忆由业务后端注入，记忆写入以 buffer 形式随响应返回，由后端异步落库。这让算法服务可以随意水平扩容。

## 意图路由：BERT 三分类

对话系统里有三类能力——**业务 Agent**（产品功能、账号）、**心理健康 Agent**（情绪陪伴、心理咨询）、**通用 Agent**（闲聊、常识）。每一轮用户 Query 先判断走哪一类。

这个判断在关键路径上、每轮都要跑，用 LLM 太慢也太贵。所以我基于用户后台的真实问询数据和扩展语料，微调了一个 **BERT 三分类模型**做路由：

- 类别高度不均衡（心理类样本远多于业务类），用 **focal loss + 标签平滑** 压低多数类的过度自信；

$$
\mathcal{L}_{\text{focal}} = -\alpha\,(1 - p_t)^{\gamma}\,\log p_t
$$

- **关键词规则兜底**：能被明确规则命中的（出现产品名、功能词）直接短路，不进模型；
- 保留一个「低置信」出口：模棱两可的样本回退到通用 Agent，而不是硬猜。

一次轻量前向替代一次 LLM 调用，路由的延迟和成本都可控，多 Agent 系统整体的响应效率随之提升。

## 情绪路由 Agent

路由不是简单的"选一个 agent"，而是拆成三个阶段，各自用一个小模型完成：

| 阶段 | 职责 | 产出 |
| --- | --- | --- |
| **会话摘要** | 增量维护当前会话的情绪脉络与关键事件 | `session_summary` |
| **情绪信号** | 判断这一轮情绪是否显著变化、是否需要回流到路由 | `emotion_signal` |
| **角色推荐** | 结合摘要 + 信号 + 14 个 agent 的画像，给出「陪伴角度」建议 | 推荐 agent + 引导话术 |

关键决策：**路由只做软建议，不强制切换**。识别到当前角色不匹配时，返回"要不要试试 X"给前端，由用户自己选。原因是把一个求安慰的孩子自动转给"毒舌吐槽"角色，代价太高。

路由和子 Agent 之间通过 **handoff summary** 交接——子 Agent 拿到的不是整段历史，而是路由蒸馏过的一小段上下文，避免人格"串味"。当情绪信号检测到明显转折（比如从"愤怒"转向"低落"），触发**情绪回流**，重新回到路由阶段重新推荐。

## 14 个「物件人格」Agent

产品把每种陪伴方式拟人化成一个物件：吸泪纸巾盒（只递纸巾不讲道理）、顺毛小梳子（无条件站你这边）、出气小沙袋（一起吐槽不劝和）、恒温小毛毯（缓解孤独）……每个角色有明确的**陪伴角度**和**红线**。

工程上：

- 每个角色是一个**独立的 LangGraph 子图**（`graph.py` / `llm.py` / `prompts.py` / `states.py`），共享一套 common 基类；
- 角色元信息（画像、场景、采样参数、是否带技能）集中在一份 registry，路由阶段直接读这份画像做推荐；
- 部分角色挂**技能**——HTML 小游戏（呼吸泡泡、出气沙袋等），由 Agent 在合适时机推给前端，文件走 CDN 托管，算法服务本地不需要这些资源也能跑。

新增一个角色 = 加一份子图 + 一条 registry 记录，路由无需改动。

## 双层长期记忆

跨会话记住用户，用两层：

| 层 | 载体 | 存什么 |
| --- | --- | --- |
| **语义记忆** | 轻量化 Mem0 框架 | 向量化的长期事实，按相似度检索 |
| **结构化记忆** | SQLite | 用户画像 + 对话 episode，可精确查询 |

两层都通过**异步事实抽取**更新：一轮对话结束后，在主线程外让 LLM 从对话里抽出昵称、当前情绪、聊到的主题等信息写回记忆，不阻塞下一轮响应。对话开始时把两层拼成一段紧凑的「用户记忆块」注入上下文，让角色 Agent 一上来就知道「这是谁、最近在经历什么」。

## 安全：两级危机检测

这是产品的安全底线，贯穿所有角色：

```text
用户消息
  │
  ├─ 关键词层：扫"自杀 / 自残 / 活着没意思"等强信号
  │     未命中 → 正常对话
  │     命中 ↓
  ├─ LLM 复核层：区分真实求助 vs 吐槽 / 玩笑（"作业多到想死"≠ 危机）
  │     confirmed = False → 正常对话
  │     confirmed = True ↓
  └─ 固定转介话术（模板，非 LLM 生成）：
        共情承接 + 全国心理援助热线 + 急救提示 + 免责声明
        → 绕过常规生成直接返回
```

设计要点：**宁可误触发多打一次 LLM，也不能漏**；**援助热线和转介话术绝不交给 LLM**——一个幻觉出来的错误号码可能让求助失败。

## 复盘

- 这个项目让我第一次完整地把"多 Agent"从 demo 做到线上：难点不在单个 Agent，而在**路由、交接、回流、记忆、安全**这些"胶水"，以及它们在无状态约束下如何组织。
- **拟人化是产品的，不是技术的**——真正让陪伴有效的是每个角色清晰的"红线"和"陪伴角度"，prompt 里写"会说'你这题怎么又错了'但不会人身攻击"，比写"严厉"有用得多。
- 危机检测这条线上，我更愿意接受工程上的"笨"（关键词 + 固定模板）换取确定性。有些地方就是不该让模型自由发挥。
